# GroupMQ Core Concepts (心智模型)

本文档描述了 GroupMQ 的核心设计理念、层级结构以及基于状态流转的调度决策逻辑。

## 1. 核心层级结构 (Hierarchy)

系统由外向内分为三层：

1.  **Queue (队列)**
    *   Redis 中的命名空间 (Namespace)，是所有资源的容器。
2.  **Group (分组)**
    *   **核心概念**：任务的逻辑隔离单元和并发控制的**状态实体**。
    *   它不仅隔离任务，还通过自身的“状态”决定是否允许被 Worker 处理。
3.  **Job (任务)**
    *   实际执行的最小原子单元，通过 `orderMs` 维护在组内的绝对顺序。

---

## 2. 组的状态机模型 (Group State Machine)

为了在大规模分组场景下保持高性能，GroupMQ 引入了“准入感知”调度。Group 会根据其**活跃任务数**与**并发限制 (`concurrency`)** 的关系，在以下集合中自动流转：

### 组的状态分类
*   **Ready (就绪态)**: 
    *   **定义**：组内有任务，且当前**活跃任务数 < 并发限制**。
    *   **行为**：Worker 只会扫描处于该状态的组。
*   **Limited (受限态/饱和态)**: 
    *   **定义**：组内有任务，但当前**活跃任务数 ≥ 并发限制**。
    *   **行为**：组被暂时移出扫描范围。这避免了 Worker 浪费 CPU 去尝试一个已经“满载”的组，解决了“无效扫描”问题。
*   **Idle (空闲态)**: 
    *   **定义**：组内没有任何待处理任务。

### 核心状态流转触发点
*   **预留 (`reserve`)**: 当 Worker 取走任务导致活跃数达到上限时，组从 `Ready` 自动进入 `Limited`。
*   **完成/重试 (`complete/retry`)**: 任务释放位子后，若组处于 `Limited` 且仍有剩余任务，组会立即移回 `Ready`。
*   **入队 (`enqueue`)**: 新任务进入空组或有空位的组时，组进入 `Ready`；若进入已满载的组，则进入 `Limited`。

---

## 3. 调度决策模型 (Dispatching)

Worker 获取任务的过程遵循“先选组，再取任务”的原则：

### 维度一：组间调度 (Inter-Group)
*决定 Worker 在所有 **Ready 状态** 的组中选择哪一个？*

*   **默认模式 (FIFO)**: 严格按照各组最老任务的 `score` 排序，确保全局公平。
*   **策略模式 (With Strategy)**:
    *   **RoundRobin**: 对 `Ready` 组进行洗牌，防止某个大组利用 Worker 并发优势抢占所有资源。
    *   **Priority**: 结合 `Ready` 状态与组配置中的 `priority`。只有“有空位”的组才有资格参与优先级竞争。

### 维度二：组内调度 (Intra-Group)
*选中某个 Group 后，处理该组内的哪个任务？*

*   **策略**: 严格基于 `orderMs` + `seq` (序列号)。
*   **逻辑**: 即使组支持并行（`concurrency > 1`），任务的派发顺序依然严格锁定。

---

## 4. 并发控制模型 (Concurrency)

系统通过双层控制实现“背压感知”：

1.  **Worker 级并发 (`Worker.concurrency`)**: 
    *   控制单个 Worker 进程同时处理的协程数，保护本地计算资源。
2.  **Group 级并发 (`Group.concurrency`)**: 
    *   **物理层**: 通过 Redis List (`:active`) 记录集群中该组正在运行的任务 ID。
    *   **调度层**: 通过 `Limited Set` 物理隔离满载的组。当某个组的处理速度跟不上生产速度时，它会自动进入“静默排队”状态，不会对其他组的调度造成毫秒级的阻塞或干扰。

---

## 5. 总结

GroupMQ 的核心价值在于 **“高效的组调度”**。

通过将并发控制下沉到 **Ready/Limited 集合流转** 中，我们将调度逻辑从“盲目尝试”转变为“精准预留”。Worker 看到的永远是“有活干且有空位”的组，这使得系统在拥有成千上万个并发受限组时，依然能保持极高的任务吞吐量和极低的延迟。